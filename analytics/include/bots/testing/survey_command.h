//
// Created by durst on 3/28/24.
//

#ifndef CSKNOW_SURVEY_COMMAND_H
#define CSKNOW_SURVEY_COMMAND_H

#include "bots/load_save_bot_data.h"
#include "bots/testing/script_data.h"
#include "bots/behavior_tree/node.h"
#include <memory>

namespace csknow::survey_command {
    /*
    struct StartRoundCommand : Node {
        vector<string> scriptLines;

        InteractiveCommand(Blackboard & blackboard, string name) : Node(blackboard, name) { };

        virtual NodeState exec(const ServerState & state, TreeThinker &treeThinker) override;
    };
     */
}

#endif //CSKNOW_SURVEY_COMMAND_H
